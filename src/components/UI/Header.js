import React from 'react';

const Header = (props) => (
    <nav>
        <div className="nav-wrapper container">
            <a href="#app" className="brand-logo">Real-Time Positions of SF Muni</a>
        </div>
    </nav>
)

export default Header